services:
  housebuild:
    build:
      context: .
      dockerfile: Dockerfile
    image: housebuild:local
    ports:
      - "5000:5000"
    environment:
      APP_HOST: 0.0.0.0
      APP_PORT: 5000
      FLASK_DEBUG: "0"
    restart: unless-stopped